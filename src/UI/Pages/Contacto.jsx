import { Text } from "@chakra-ui/react";

const Contacto = () => {
    return (
        <Text bgColor={"red.500"} fontSize={"200px"}>
            Contacto
        </Text>
    );
};

export default Contacto;
